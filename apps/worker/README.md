# Worker App

This package is the orchestration layer for Bidwright.

It composes:

- package ingestion
- retrieval indexing
- prompt generation
- reviewable AI task orchestration

The current implementation is an MVP scaffold that can already carry package summaries, phase drafting, worksheet drafting, equipment inference, and quote QA through typed interfaces.
